\documentclass[12pt,a4paper]{article}

% Поддержка русского текста для любого доступного движка LaTeX
\usepackage{iftex}
\ifPDFTeX
    \usepackage[T2A]{fontenc}
    \usepackage[utf8]{inputenc}
    \usepackage[russian]{babel}
\else
    \usepackage{fontspec}
    \usepackage{polyglossia}
    \setmainlanguage{russian}
\fi

\usepackage[
    a4paper,
    margin=2.5cm
]{geometry}

\usepackage{listings}
\usepackage{float}
\usepackage{array}
\usepackage{enumitem}
\usepackage[colorlinks=true,linkcolor=blue,urlcolor=blue,citecolor=blue]{hyperref}

\lstset{
    language=C,
    basicstyle=\ttfamily\small,
    numbers=left,
    numberstyle=\tiny,
    frame=single,
    breaklines=true,
    showstringspaces=false
}

\title{Документация к GPU Sandbox}
\author{}
\date{}

\begin{document}

\maketitle
\tableofcontents
\newpage

\section{Назначение программы}\label{sec:purpose}

GPU Sandbox представляет собой программную модель вычислительного ядра, памяти и набора инструкций, реализованную на языке C. Цель проекта --- изучение основных принципов работы процессора: адресация памяти, выполнение операций в регистрах, обработка состояний ядра, проверка корректности входных данных и симуляция исполнения программы.

На текущем этапе проект уже содержит не только базовые вычисления, но и элементы условных переходов, проверки ошибок, таймаутов, отладки и автоматического тестирования\footnote{Подробнее об устройстве ядра, памяти и логике выполнения см. в \hyperref[sec:architecture]{разделе \textit{Архитектура проекта}}.}.

\section{Архитектура проекта}\label{sec:architecture}

Модель содержит три ключевых компонента: вычислительное ядро, память и программу, представленную набором инструкций.

\begin{table}[H]
    \centering
    \caption{Ключевые компоненты архитектуры}
    \label{tab:components}
    \begin{tabular}{|p{4cm}|p{8cm}|}
        \hline
        \textbf{Компонент} & \textbf{Назначение} \\
        \hline
        \texttt{Memory} & Виртуальная память, хранящая данные и обеспечивающая чтение/запись по адресам. \\
        \hline
        \texttt{Core} & Вычислительное ядро с регистрами, счётчиком команд и состояниями выполнения. \\
        \hline
        \texttt{Instruction} & Структура команды, задающая тип операции и аргументы \texttt{a}, \texttt{b}, \texttt{c}. \\
        \hline
        \texttt{Simulator} & Цикл выполнения программы с контролем состояния ядра, ошибок и таймаута. \\
        \hline
    \end{tabular}
\end{table}

\subsection{Память}

\texttt{Memory} --- это структура виртуальной памяти, хранящая данные в массиве
\texttt{data[MEMORY\_SIZE]}.

Память используется для хранения значений, которые считываются и записываются в ходе выполнения программы. На данный момент размер памяти определяется константой
\texttt{MEMORY\_SIZE = 1024 * 1024}.

Операции чтения и записи реализованы следующим образом:

\begin{itemize}[leftmargin=1.5em]
    \item \texttt{memory\_write(Memory *memory, uint32\_t address, uint8\_t value)} --- запись байта в ячейку памяти;
    \item \texttt{memory\_read(const Memory *memory, uint32\_t address)} --- чтение байта из памяти;
    \item \texttt{memory\_reset(Memory *memory)} --- очистка памяти до нуля.
\end{itemize}

Проверка допустимости адреса выполняется через специальный валидатор, который запрещает обращение за пределы памяти.

\subsection{Ядро процессора}

\texttt{Core} --- структура вычислительного ядра. В ней хранятся регистры, программный счётчик, текущее состояние и флаг сравнения.

\subsubsection{Регистры}

\texttt{registers[REGISTERS\_COUNT]} --- массив регистров, который используется для промежуточных вычислений и передачи данных между инструкциями.

Количество регистров задаётся константой
\texttt{REGISTERS\_COUNT}, который, на данный момент, равен 8.

\subsubsection{Программный счётчик}

\texttt{pc} (программный счётчик) --- счётчик, который хранит индекс текущей выполняемой команды. После выполнения инструкции счётчик изменяется в соответствии с состоянием ядра.

\subsubsection{Флаги состояния}

Внутри ядра присутствует переменная \texttt{flag}, которая используется для результатов сравнения. В проекте применяются следующие значения:

\begin{itemize}[leftmargin=1.5em]
    \item \texttt{SAME} --- значения равны;
    \item \texttt{GREATHER} --- первое значение больше второго;
    \item \texttt{LESS} --- первое значение меньше второго.
\end{itemize}



\subsubsection{Состояния ядра}

\texttt{current\_state} управляет поведением выполнения программы. Набор состояний представлен в перечислении \texttt{State}:

\begin{enumerate}[leftmargin=2em]
    \item \texttt{NORMAL} --- обычный режим работы ядра;
    \item \texttt{JUMP} --- выполнение перехода на другую инструкцию;
    \item \texttt{HALT} --- завершение работы программы;
    \item \texttt{ERROR} --- аварийное состояние при ошибке выполнения;
    \item \texttt{TIMEOUT} --- превышен лимит количества циклов.
\end{enumerate}

При возникновении ошибки, например, вызове некорректного регистра, адреса памяти или неизвестного оператора, состояние переводится в \texttt{ERROR}. Если же программа выполняется слишком долго, то срабатывает таймаут по переменной \texttt{MAX\_CYCLES}.

\section{Инструкции виртуального процессора}\label{sec:instructions}

Структура инструкции представлена типом \texttt{Instruction}\footnote{Подробное описание наборов операций и их логика работы представлена в \hyperref[tab:instructions]{таблице инструкций}.}:

\begin{lstlisting}[language=C]
typedef struct{
    Opcode opcode;
    uint32_t a;
    uint32_t b;
    uint32_t c;
} Instruction;
\end{lstlisting}

В проекте используются следующие значения опкодов:

\begin{enumerate}[leftmargin=2em]
    \item \texttt{OP\_LOAD}
    \item \texttt{OP\_STORE}
    \item \texttt{OP\_ADD}
    \item \texttt{OP\_SUB}
    \item \texttt{OP\_MUL}
    \item \texttt{OP\_DIV}
    \item \texttt{OP\_HALT}
    \item \texttt{OP\_JUMP}
    \item \texttt{OP\_CMP}
    \item \texttt{OP\_JIF}
\end{enumerate}

\subsection{Параметры инструкций}

В проекте применяется следующий порядок использования операндов:

\begin{itemize}[leftmargin=1.5em]
    \item \texttt{a} --- целевой регистр или индекс вычисления;
    \item \texttt{b} --- источник данных, адрес в памяти или второй аргумент;
    \item \texttt{c} --- второй источник данных при арифметических операциях.
\end{itemize}

\subsection{Поддерживаемые операции}

\begin{table}[H]
    \centering
    \caption{Набор инструкций}
    \label{tab:instructions}
    \begin{tabular}{|p{2.1cm}|p{2.2cm}|p{2.2cm}|p{2.2cm}|p{4.1cm}|}
        \hline
        \textbf{Операция} & \textbf{a} & \textbf{b} & \textbf{c} & \textbf{Назначение} \\
        \hline
        \texttt{OP\_LOAD} & регистр назначения & регистр с адресом & --- & Чтение значения из памяти по адресу \texttt{reg[b]} в \texttt{reg[a]} \\
        \hline
        \texttt{OP\_STORE} & регистр с адресом & регистр со значением & --- & Запись значения \texttt{reg[b]} в память по адресу \texttt{reg[a]} \\
        \hline
        \texttt{OP\_ADD} & регистр назначения & регистр 1 & регистр 2 & Сложение \texttt{reg[b] + reg[c]} и сохранение в \texttt{reg[a]} \\
        \hline
        \texttt{OP\_SUB} & регистр назначения & регистр 1 & регистр 2 & Вычитание \texttt{reg[c]} из \texttt{reg[b]} и сохранение в \texttt{reg[a]} \\
        \hline
        \texttt{OP\_MUL} & регистр назначения & регистр 1 & регистр 2 & Умножение \texttt{reg[b] * reg[c]} и сохранение в \texttt{reg[a]} \\
        \hline
        \texttt{OP\_DIV} & регистр назначения & регистр 1 & регистр 2 & Деление \texttt{reg[b] / reg[c]} с проверкой на деление на ноль \\
        \hline
        \texttt{OP\_JUMP} & индекс инструкции & --- & --- & Безусловный переход к инструкции с индексом, хранящимся в регистре \texttt{a} \\
        \hline
        \texttt{OP\_CMP} & регистр 1 & регистр 2 & --- & Сравнение значений регистров и установка флага \texttt{flag} \\
        \hline
        \texttt{OP\_JIF} & индекс инструкции & ожидаемый флаг & --- & Условный переход: если \texttt{b == core->flag}, ядро переходит по адресу \texttt{a} \\
        \hline
        \texttt{OP\_HALT} & --- & --- & --- & Завершение выполнения программы и перевод ядра в состояние \texttt{HALT} \\
        \hline
    \end{tabular}
\end{table}

Важно: фактическая реализация проекта допускает и проверяет корректность входных индексов и адресов, поэтому многие операции в случае неверного операнда переводят систему в состояние \texttt{ERROR}.

\section{Симулятор выполнения}\label{sec:simulator}

Модуль \texttt{simulator} запускает программу в цикле до тех пор, пока не будет выполнен один из следующих сценариев:\footnote{Более подробно логика работы симулятора и условия завершения описаны в \hyperref[sec:testing]{разделе \textit{Тестирование}}.}

\begin{itemize}[leftmargin=1.5em]
    \item достигнут конец программы;
    \item ядро перешло в состояние \texttt{HALT};
    \item возникла ошибка;
    \item число выполненных циклов превысило лимит \texttt{MAX\_CYCLES}.
\end{itemize}

В проекте глобальная переменная \texttt{cycles} хранит количество уже выполненных инструкций, а константа \texttt{MAX\_CYCLES}, на данный момент, равна 1\,000\,000.

Схема выполнения выглядит так:

\begin{enumerate}[leftmargin=2em]
    \item выбирается текущая инструкция по индексу \texttt{pc};
    \item она исполняется функцией \texttt{execute};
    \item состояние ядра проверяется на наличие ошибки;
    \item если состояние \texttt{NORMAL}, счётчик увеличивается на единицу;
    \item если состояние \texttt{JUMP}, оно сбрасывается обратно в \texttt{NORMAL};
    \item после каждого цикла увеличивается счётчик \texttt{cycles};
    \item при превышении лимита вызывается состояние \texttt{TIMEOUT}.
\end{enumerate}

\section{Проверка корректности входных данных}\label{sec:validation}

Для контроля ошибок проект использует набор валидаторов, реализованных в модуле инструкций\footnote{Подробнее о логике ошибок и проверках см. \hyperref[sec:simulator]{раздел \textit{Симулятор выполнения}}.}. Основные проверки:

\begin{itemize}[leftmargin=1.5em]
    \item \texttt{checker\_destination} --- проверка допустимого индекса целевого регистра;
    \item \texttt{checker\_address\_register} --- проверка адресного регистра;
    \item \texttt{checker\_source\_register} --- проверка регистра-источника;
    \item \texttt{checker\_condition\_register} --- проверка регистра условия;
    \item \texttt{checker\_target\_register} --- проверка регистра перехода;
    \item \texttt{checker\_memory\_address} --- проверка корректности адреса памяти;
    \item \texttt{checker\_instruction\_index} --- проверка индекса инструкции в пределах программы.
\end{itemize}

В случае некорректных данных ядро переводится в состояние \texttt{ERROR}, а в консоль выводится сообщение о причине сбоя.

\section{Отладочный вывод}\label{sec:debug}

Проект содержит набор функций для вывода состояния виртуального процессора:\footnote{Функции отладки напрямую связаны с режимами работы ядра, описанными в \hyperref[sec:architecture]{разделе \textit{Архитектура проекта}}.}

\begin{itemize}[leftmargin=1.5em]
    \item \texttt{print\_registers} --- печать всех регистров;
    \item \texttt{print\_state} --- печать текущего состояния ядра;
    \item \texttt{print\_program} --- отображение программы с операциями и аргументами.
\end{itemize}


\section{Сброс и инициализация}\label{sec:reset}

Для повторного использования модели предусмотрены функции сброса:

\begin{itemize}[leftmargin=1.5em]
    \item \texttt{core\_reset(Core *core)} сбрасывает регистры в ноль, обнуляет счётчик \texttt{pc} и возвращает состояние ядра в \texttt{NORMAL};
    \item \texttt{memory\_reset(Memory *memory)} очищает память.
\end{itemize}

Это позволяет запускать несколько тестовых сценариев последовательно на одном и том же экземпляре модели.

\section{Тестирование}\label{sec:testing}

В папке \texttt{tests} находятся отдельные программы, проверяющие корректность работы ключевых компонентов:\footnote{См. также \hyperref[sec:validation]{раздел \textit{Проверка корректности входных данных}}.}

\begin{itemize}[leftmargin=1.5em]
    \item \texttt{test\_core.c} --- проверяет сброс ядра;
    \item \texttt{test\_memory.c} --- проверяет запись, чтение и очистку памяти;
    \item \texttt{test\_instruction.c} --- проверяет все основные инструкции и проверки ошибок;
    \item \texttt{test\_debug.c} и \texttt{test\_simulator.c} --- покрывают дополнительные сценарии отладки и симуляции.
\end{itemize}

Автотесты написаны через макросы \texttt{assert}, что позволяет проверять корректность поведения программы на уровне условий и побочных эффектов.


\end{document}

